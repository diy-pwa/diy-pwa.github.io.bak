import Nav from './Nav.jsx';
export const menuItems = {
  text: 'Logo',
  to: '/',
  items: [{ text: 'Contact', to: 'contact', icon: 'fa fa-envelope' }],
};

<header className="w3-display-topleft w3-xlarge">
  <Nav navData={menuItems} />
</header>
